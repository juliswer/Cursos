Hay 3 tipos:
Transformadores, accesores y de repeticion.

Transformadores:
pop() - Elimina el ultimo elemento de un array y lo devuelve.
shift() - Elimina el primer elemento de un array y lo devuelve.
push() - Agrega un elemento al array al final de la lista.
reverse() - Invierte el orden de los elementos de un array.
unshift() - Agrega uno o mas elementos al inicio del array, y devuelve la nueva longitud del array.
sort() - Ordena los elementos de un arreglo (array) localmente (es decir, alfabeticamente) y devuelve el arreglo ordenado.
splice() - cambia el contendo de un array eliminando elementos existentes y/o agregando nuevos elementos. Con el valor de la izquierda se indica la posicion en la que se elimina, y el de la derecha la cantidad de elementos que elimina

Accesores:
join() - Une todos los elementos de una matriz (u objeto similar) en una cadena y lo devuelve. (Une todos los elementos y los transforma en un string)
slice() - Devuelve una parte del array dentro de un nuevo array empezando por inicio hasta fin (fin no concluido) 
tostring(), indexOf(), lastIndexOf(), includes() - El uso esta explicado en el archivo de cadenas.

De repeticion:
filter() - Crea un nuevo array con todos los elementos que cumplan la condicion.
forEach() - Ejecuta la funcion indicada una vez por cada elemento del array.